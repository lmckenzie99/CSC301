The value of w is 4. Function g is invoked from "let w = g(f,2).", which then splits the argument passed to it.
(f,2) takes the form q(p), which when returned from g becomes f(2), invoking function f with argument 2.
function f returns its argument multiplied by itself, so f(2) returns 4.
